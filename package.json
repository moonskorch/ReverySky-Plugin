{
  "name": "reverysky-obsidian-plugin",
  "version": "1.1.0",
  "private": true,
  "description": "ReverySky Map Obsidian plugin",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "check:unity-webgl": "node scripts/check-unity-webgl-artifacts.mjs",
    "check:plugin-release": "node scripts/check-plugin-release-artifacts.mjs",
    "build": "npm run package:folder-runtime",
    "package:folder-runtime": "npm run check:unity-webgl && tsc --noEmit --skipLibCheck && node esbuild.config.mjs production && node scripts/package-mode-marker.mjs folder-runtime && npm run check:package:folder-runtime",
    "package:embedded-html": "npm run check:unity-webgl && tsc --noEmit --skipLibCheck && node esbuild.config.mjs production && node scripts/package-embedded-html.mjs && npm run check:package:embedded-html",
    "package:embedded-archive": "npm run check:unity-webgl && tsc --noEmit --skipLibCheck && node esbuild.config.mjs production && node scripts/package-embedded-archive.mjs && npm run check:package:embedded-archive",
    "package:release-candidate": "npm run package:embedded-archive",
    "measure:embedded-archive": "node scripts/measure-embedded-archive-size.mjs",
    "check:package:folder-runtime": "node scripts/check-package-folder-runtime.mjs",
    "check:package:embedded-html": "node scripts/check-embedded-html-package.mjs",
    "check:package:embedded-archive": "node scripts/check-embedded-archive-package.mjs",
    "check:release-metadata": "node scripts/check-release-metadata.mjs",
    "check:package:release-candidate": "npm run check:package:embedded-archive && npm run check:release-metadata",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ui-visual": "playwright test tests/visual",
    "test:ui-visual:update": "playwright test tests/visual --update-snapshots",
    "test:ui-visual:report": "playwright show-report"
  },
  "dependencies": {
    "tar": "^7.5.16"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@types/node": "^20.16.5",
    "esbuild": "^0.28.0",
    "jsdom": "^26.1.0",
    "obsidian": "^1.8.10",
    "typescript": "^5.6.2",
    "vitest": "^4.1.7"
  }
}
